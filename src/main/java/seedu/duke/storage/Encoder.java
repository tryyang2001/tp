package seedu.duke.storage;


import java.util.ArrayList;

public class Encoder {
    protected ArrayList<String> detailsToSave = new ArrayList<>();

//    /**
//     * Encodes the details to save into strings for storage.
//     *
//     * @param toFileString The strings to be encoded into the storage files.
//     * @return An ArrayList of the profile details to be stored
//     */
//    public ArrayList<String> encode(String[] toFileString) {
//        ArrayList<String> detailsToSave = new ArrayList<>();
//        for (int i = 0; i < toFileString.getSize(); i++) {
//            detailsToSave.add(foodList.getItem(i).toFileTextString());
//        }
//        return detailsToSave;
//    }
}
